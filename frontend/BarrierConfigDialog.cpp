#include "BarrierConfigDialog.h"

// Implementation is in the header (inline), but this file is needed for Qt's moc to generate meta-object code.

// If you add non-inline methods, implement them here.
